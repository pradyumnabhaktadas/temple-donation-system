# provides pkg_resources, which razorpay's client.py imports directly --
# Python 3.12+ venvs no longer bundle setuptools by default. Capped below
# 82: setuptools removed pkg_resources entirely as of v82.0 (Feb 2026), so
# an unpinned upper bound installs a version that's missing it again.
setuptools<82
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Migrate==4.0.7
razorpay==1.4.2
requests==2.32.3   # also used directly by whatsapp_utils.py (Meta WhatsApp Cloud API calls)
reportlab==4.2.2
python-dotenv==1.0.1
pytest==8.3.3
gunicorn==22.0.0
sentry-sdk[flask]==2.13.0
psycopg2-binary==2.9.9   # Postgres driver -- only used if DATABASE_URL points at Postgres (e.g. on Render)
Flask-Limiter==3.8.0     # rate limiting on the public donation API
Flask-Talisman==1.1.0    # security headers (HSTS/CSP/X-Frame-Options/etc.), production only
openpyxl==3.1.5           # reads .xlsx uploads -- see _table_from_xlsx in admin.py
